import 'package:flutter/material.dart';

class SequenceSelector extends StatelessWidget {
  const SequenceSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
